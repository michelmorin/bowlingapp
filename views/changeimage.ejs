<% include partials/header %>
<div class="row">
    <div class="col s10 offset-s1 m8 offset-m2 l8 offset-l3 xl5 offset-xl3">
        <div class="card">
            <div class="card-content">
                <% if (sessionFlash && sessionFlash.imageAlertMsg ) { %>
                    <div class="chip <%= sessionFlash.type %> alert-box">
                        <i class=" close material-icons right white-text">close</i>
                        <% if (sessionFlash.imageAlertMsg) { %>
                            <h6 class="white-text"><%= sessionFlash.imageAlertMsg %><h6>
                        <% } %>
                        </div>
                    </div>
                <% } %>
                <h4 style="margin-top:0px;text-align:center">Change Image</h4>
                <div class="divider"></div>
                <br>
                <form action="/user/<%= user._id %>/upload" method="post" encType="multipart/form-data">
                    <div class="row">
                        <div class="center col s12">
                            <!--<div class="input-field col s12" style="margin-bottom:20px">
                                <i class="fas fa-camera prefix"></i>
                                <label for="image">Image URL</label>
                                <input type="url" class="validate" name="image" id="image" value="<%= user.image %>" required/>
                                <% if (sessionFlash && (sessionFlash.imageErrorMsg) ) { %>
                                    <span class="helper-text" data-error="<%= sessionFlash.imageçErrorMsg %>"></span>
                                <% } %> 
                            </div>-->
                            <div class="file-field input-field">
                                <div class="btn">
                                <span>File</span>
                                <input type="file" name="file">
                                </div>
                                <div class="file-path-wrapper">
                                <input class="file-path validate" type="text" placeholder="">
                                </div>
                            </div>
                        </div>
                        <div class="center col s12" style="margin-bottom:20px;margin-top:40px">
                            <button style="width: 100%" class="btn btn-large waves-effect waves-light blue" type="submit">Save</button>
                        </div>
                        <div class="center col s12">
                            <a href="/user/<%= user._id %>/edit" style="width: 100%" class="btn btn-large waves-effect waves-light black">Back</a>
                        </div>
                    </div>
                </form>
            </div>    
        </div>
    </div>
</div>
<% include partials/footer %>

